#Docker :
docker exec -it --user root <container_name_or_id> /bin/bash

docker-compose down
docker-compose up --build
docker-compose ps
docker-compose logs <container>

#Post payload
curl -X POST http://RasaxDocker_actions:5055/webhook -H "Content-Type: application/json" -d @payload.json
